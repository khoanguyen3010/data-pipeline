sources:
- host: ENV:NOTES_DB_HOST
  user: ENV:NOTES_DB_USER
  password: ENV:NOTES_DB_PASS
  databases:
    staging_coach_notes:
      schemas:
        public:
          truncate: []
          exclude: []
